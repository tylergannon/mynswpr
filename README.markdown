# Mine Sweeper!!

* Built using SCSS and CoffeeScript, compiled to clean HTML/CSS/JavaScript.
* Play it at http://tylergannon.github.com/minesweeper.html

* Look inside `public/assets/application.js` and `public/assets/application.css` for the application.
* For an easier look, check the SCSS and CoffeeScript inside `app/assets`.

* Full specs for the Game class and some others... inside `spec/javascripts`
